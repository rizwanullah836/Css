#include<iostream>
using namespace std;
struct node{
	int data;
	node* next;
	node* prev;
};
node* head=NULL;
void insert(int value){
	node* n = new node();
	n->data = value;
	n->next= head;
	n->prev=NULL;
	if(head!=NULL){
		head->prev=n;
		
	}
	head = n;
}
void print(){
	node* temp = head;
	while(temp!=NULL){
		cout<<temp->data<<" ";
		temp = temp->next;
	}
}
int main(){
	insert(55);
	insert(6);
	insert(7);
	insert(8);
	insert(9);
	print();
}
